# Precompiled python modules
*.pyc

# Eagle backups
*.s#?
*.b#?

# Gerber files
*.CUT
*.GBL
*.GBO
*.GBS
*.GML
*.GTL
*.GTO
*.GTP
*.GTS
*.TXT
*.dri
*.gpi
*.pro

# LaTeX compiled file
*.pdf
*.log
*.aux

# OS X crap
.DS_Stor?

# Working notes, not for the public repository. These stay on disk (and in
# iCloud) but are deliberately not published: they are the running engineering
# record and planning material, not documentation for users.
/.claude/
/AGENTS.md
/CLAUDE.md
/docs/KZ_AMBIKA_CODEX_HANDOVER.md
/docs/VOICECARD_V2_PLAN.md
/docs/REPOSITORY_CONSOLIDATION.md
/docs/STABLE_BASELINE.md

# Local compiler output. Reviewed firmware packages live in KZ-firmware_builds.
/build/
*.size
__pycache__/

# Keep the build evidence shipped with reviewed firmware packages. The Eagle
# and Gerber rules above are case-insensitive on macOS, so *.TXT would
# otherwise swallow the section sizes and symbol table the manifests hash.
!/KZ-firmware_builds/**/build.log
!/KZ-firmware_builds/**/size.txt
!/KZ-firmware_builds/**/symbols.txt

# KiCad local backups and lock files
voicecard/hardware_design/pcb/KiCad/.history/
voicecard/hardware_design/pcb/KiCad-SMD/.history/
*.lck
